DECLARE
STR VARCHAR2(16):='0123456789ABCDEF';
NO NUMBER;
BASE NUMBER(2);
REM VARCHAR2(100);
ANS VARCHAR2(100);
BEGIN
NO:=&NO;
BASE:=&BASE;
WHILE ( NO > 0 )
LOOP
	REM:=MOD(NO,BASE);
	ANS:=SUBSTR(STR,REM+1,1)||ANS;
	NO:=TRUNC(NO/BASE);
END LOOP;
	DBMS_OUTPUT.PUT_LINE(ANS);
END;
/
