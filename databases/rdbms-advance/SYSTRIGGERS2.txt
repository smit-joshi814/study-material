CREATE OR REPLACE TRIGGER ACTIVITY 
AFTER CREATE OR ALTER OR DROP ON DATABASE
BEGIN

INSERT INTO ACTIV_INF VALUS(
SYS.LOGIN_USER,
SYSDATE,
SYS.DICTONARY_OBJ_TYPE,
SYS.DICTONARY_OBJ_NAME,
SYS.SYSEVENT()
);

END ACTIVITY;