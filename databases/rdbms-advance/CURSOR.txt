DECLARE
CURSOR C1 IS SELECT CITY,COUNT(RATING)"TOTAL" FROM CUSTOMER GROUP BY CITY;
C C1%ROWTYPE;
BEGIN
	OPEN C1;
	LOOP
		FETCH C1 INTO C;
		EXIT WHEN C1%NOTFOUND;
		
		DBMS_OUTPUT.PUT_LINE(C.CITY||' '||C.TOTAL);
	END LOOP;
	CLOSE C1;
END;	